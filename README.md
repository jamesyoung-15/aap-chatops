# Ansible Automation Platform (AAP) ChatOps

Python-based ChatOps bridge that connects chat platforms (Slack, Teams, etc.) to Ansible Automation Platform (AAP). Listens for commands/messages and runs AAP API calls to perform actions such as current pending workflow approvals, checking the status of all jobs a user has run today, etc.
